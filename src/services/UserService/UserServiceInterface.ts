export interface GetUserResponse {
    name: string;
    lastName: string;
    birthDay: string;
}

export interface User {
    name: string;
    lastName: string;
    birthDay: string;
    documentType: string;
    documentNumber: string;
    phone: string;
}